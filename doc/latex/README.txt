This documentation is made with LaTeX. Make sure to have a LaTeX Compiler installed in your computer.
For this you can install texlive using the command :
"sudo apt update && sudo apt upgrade && sudo apt install texlive-full"
Then compile the .tex using :
"pdflatex main.tex"
Notice : Make sure to press Enter everytime you see a warning or an error